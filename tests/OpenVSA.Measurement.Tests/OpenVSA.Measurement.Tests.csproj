<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>OpenVSA.Measurement.Tests</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\OpenVSA.Measurement\OpenVSA.Measurement.csproj" />

    <!--
      REQ-LIM-003 is a criterion about the automation API, so the tests for it need it. A test-only
      reference; nothing in OpenVSA.Measurement references OpenVSA.Api, which sits above it.
    -->
    <ProjectReference Include="..\..\src\OpenVSA.Api\OpenVSA.Api.csproj" />

    <!--
      For SyntheticSymbolSource: the modulated generator REQ-SIM-001 delivered, already proved
      against the parameters it is asked for. A demodulation test needs a modulated signal, and
      writing a second generator to get one would be a second thing to keep correct.
    -->
    <ProjectReference Include="..\..\src\OpenVSA.TestHarness\OpenVSA.TestHarness.csproj" />
  </ItemGroup>

</Project>
